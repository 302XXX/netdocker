# NetDocker — зависимости
# Установка:  pip install -r requirements.txt
#
# Версии указаны нижней границей (>=) для совместимости; при необходимости
# зафиксируйте точные версии (==) для воспроизводимых сборок.

# DNS wire-format: парсинг/сборка запросов и локальный DNS-сервер
dnslib>=0.9.24

# DoH-запросы (DNS-over-HTTPS) к xbox-dns.ru
requests>=2.28.0

# Мониторинг процессов и работа с системой
psutil>=5.9.0

# Иконка в системном трее (только GUI-режим)
pystray>=0.19.4

# Рендер иконки трея (зависимость pystray)
Pillow>=9.0.0

# ── Опционально ──────────────────────────────────────────────────────────────
# DoQ (DNS-over-QUIC). Без него DoT/DoH/UDP работают как обычно, а выбор DoQ
# даёт понятную ошибку и fallback. Раскомментируйте, если нужен DoQ:
# aioquic>=1.0.0
#
# DNSCrypt (для пользовательских sdns:// серверов). Без него остальные
# транспорты работают как обычно, а выбор DNSCrypt даёт понятную ошибку.
# pynacl>=1.5.0
#
# Примечание: DoT (DNS-over-TLS) работает БЕЗ дополнительных пакетов
# (используется стандартный модуль ssl).
